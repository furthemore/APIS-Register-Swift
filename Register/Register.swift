//
//  Register.swift
//  Register
//

import SwiftUI
import os

struct Register {
  static let logger = Logger(subsystem: "net.syfaro.Register", category: "Main")

  static let themeColor = Color(red: 0, green: 153 / 255, blue: 204 / 255)
  static let fallbackURL = URL(string: "https://www.google.com")!
}
